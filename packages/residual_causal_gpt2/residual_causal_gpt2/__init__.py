"""Residual Causal Toolkit — GPT-2 residual stream package.

Phases:
  A. Frozen residual ports on a trained/pretrained GPT-2 residual stream
  B. Residual component trajectory during training
  C. Closed-loop residual coefficient control

Primitives (domain-agnostic residual causal protocol):
  contrastive residual extraction
  path-patch / zero / random / residual-atom controls
  parallel residual observation streams R1/R2/R3 with original-S isolation
  pure-quadratic 14D residual expansion
"""
__version__ = "0.1.0"
